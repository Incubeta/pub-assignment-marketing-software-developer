"""
Create a class that inherets from the ApiManager class
In this class we want to have Class Members that:
    1. Can handle different languages via the header Accept-language
    2. Can only handle the languages "nl", "fr" and "en" and must throw an exception
       when another language is being set
    3. Can get a list of hotels from the endppoint 'catalog/v1/inventory/hotels'
    4. Can get information of a specific hotel on 'catalog/v1/inventory/hotels/${id}'

The below needs to be ajusted
"""

class OpenApiManager:
    pass